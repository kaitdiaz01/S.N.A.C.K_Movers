       <div class="card rounded">
                <div class="card-body">
                    <div id="intro" class="bg-image shadow-2-strong">
                        <div class="mask" style="background-color: rgba(0, 0, 0, 0.8);">
                          <div class="container d-flex align-items-center justify-content-center text-center h-100">
                            <a href="{{id}}"></a>
                            <div class="text-white">                           
                              <h3 class="m-4">Hotel Name: {{name}}</h3>
                              {{!-- <h3 class="m-4">{{summary}}</h3> --}}
                              {{!-- <h3 class="m-4">School website:</h3><a href="{{web-site}}">{{web-site}}</a> --}}
                              <h3 class="m-4">Rating: {{rating}}</h3>
                              <h3 class="m-4">{{display_address}}</h3>
                              {{!-- <img href="{{image_url}}" > --}}
                              <a class="btn btn-outline-light btn-med m-2" href= {{url}}role="button"
                                rel="nofollow"> Navigate to
                                {{name}} Website</a>
                            </div>
                            </a>
                          </div>
                        </div>
                      </div>
                </div>
              </div>